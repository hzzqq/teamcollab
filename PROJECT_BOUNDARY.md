# Project4 边界划定（不可动摇）

本文件是 project4 的**范围宪法**。任何 PR / 迭代 / 需求，若触碰"拒绝清单"，
除非老板（用户）明确书面放行，否则**直接拒绝**，不做"先做着看"。

---

## 一句话定位

TeamCollab 是一个**团队协作 SaaS 产品**：看板 + 任务 CRUD + 评论/@提及 + 通知
+ 权限（owner/admin/member/viewer）+ 多租户隔离。

它是**一个产品**，不是一个平台。

---

## 核心边界（死线）

1. **只做协作产品**：围绕"任务 / 看板 / 成员协作 / 权限 / 租户"这一核心闭环打磨，
   不做横向扩张。
2. **禁止变成多工具平台**：不引入"应用市场 / 插件体系 / 通用搭建器 / 低代码 /
   流程引擎"等把本产品变成宿主平台的架构。
3. **不重复 project3**：project3 是"热门软件 Top100 调研报告"（数据分析 / 报告生成
   管线），与本项目零功能重叠。禁止把研究 / 分析 / 报表类工具塞进 project4。
4. **扩展位 ≠ 现在就做**：README 提到的"文档协作 / 即时沟通 / 日程"仅作为**未来
   可能的方向占位**，当前 MVP 阶段一律不做，不允许以"预留扩展"为名提前开发。

---

## 明确不在范围内（拒绝清单）

- 文档协同编辑（Notion-like）
- 即时通讯 / 群聊（Slack-like）
- 日历 / 日程管理
- 文件网盘 / 对象存储服务
- 通用 BI / 数据看板 / 报表引擎
- 低代码 / 流程引擎 / 应用搭建器
- 把鉴权、存储等拆成独立对外 SaaS

---

## 与 project3 的分工

| 项目 | 性质 | 职责 |
|------|------|------|
| project3 | 调研 / 分析 | 热门软件选品研究、数据管线、报告（Python 脚本） |
| project4 | 产品 | 团队协作 SaaS（本仓库） |

两者边界清晰，互不跨界。若某需求疑似落在 project3 领域，应回到 project3 实现，
而非在 project4 内造轮子。

---

## 越界处置

任何开发任务若落入"拒绝清单"，**默认打回**。如确属业务必需，须由老板显式确认
"临时放行"并登记原因，方可进入 Sprint；放行项不写入本文件的死线，单独记录于
`docs/` 下的决策日志。
